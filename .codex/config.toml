# Kumo Cross-Provider Orchestrator Configuration
# Automatically synchronized with active user settings.
# Active Orchestrator: chatgpt-6-astra (low)
# Active Worker:       gpt-5.6-luna (medium)

model = "chatgpt-6-astra"
model_reasoning_effort = "low"

approval_policy = "on-request"

[agents]
enabled = true
max_concurrent_threads_per_session = 4

[mcp_servers.gemini-bridge]
command = "node"
args = ["src/bridge/server.js"]
env = {}
